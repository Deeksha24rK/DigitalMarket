/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            "localhost",
            "digitalmarket-production-6bbe.up.railway.app"
        ]
    }
};

export default nextConfig;
